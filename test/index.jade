extends layout/default

append scripts
  script
    var Modal = require('modal');
    var $ = require('jquery');

    $('.ui.modal.manual-init').each(function (index, el) {
      var modal = new Modal(el);
    });

append content
  h1 Modal

  a(href="#", data-open-modal="#modal-foo") Open Modal Foo
  br
  a(href="#", data-close-modal="#modal-foo") Close Modal Foo
  br
  a(href="#", data-toggle-modal="#modal-foo") Toggle Modal Foo

  #modal-foo.ui.modal.manual-init
    .display-table.margin-auto
      .window-height.display-table-cell.vertical-align-middle
        .modal-inner
          .modal-content
            .close-modal-button
              i.icon.close.button(data-modal-trigger-close) x
            p hello foo
